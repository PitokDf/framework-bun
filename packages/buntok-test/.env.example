# Buntok Configuration
# AUTH_COOKIE: Name of the HttpOnly cookie for JWT authentication
# If set, requireAuth() will read JWT from this cookie first
# If not set or cookie not found, falls back to Authorization header
AUTH_COOKIE=token
